extends layout

block content
	h1= title
	p Book id
	form(id='reguser' action='/bookcopiesform' method='post')
		div Select id of a book
			select(name='bks')
				each book in books
					option #{books.id}
		input(type='number' name='id' required)
		input(type='submit' value='Go')